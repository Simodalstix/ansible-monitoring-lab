all:
  children:
    webservers:
      hosts:
        app-server:
          ansible_host: 192.168.1.100  # Change to your VM IP
          ansible_user: ubuntu          # Change to your SSH user
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
  vars:
    ansible_python_interpreter: /usr/bin/python3